# Cascade Projects

Private workspace for Cascade-managed projects.

- **Owner:** dcatlin7921
- **Scope:** Only `/home/dave/CascadeProjects` is tracked by git.
- **Worktrees:** This repo supports `git worktree` for parallel branch work.
